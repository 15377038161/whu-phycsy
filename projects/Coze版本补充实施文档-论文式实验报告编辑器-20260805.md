# Coze 版本补充实施文档：论文式实验报告与平台内类 Word 编辑器

> 文档用途：将 2026 年 8 月 4 日在武汉大学量子实验教学平台主版本中完成、验证并上线的功能，准确补充到 Coze 版本中。
>
> 基准提交：`aef025ceca28bcfc6083f79d8e89949a754118e6`（`feat: add structured paper report editor`）
>
> 编写日期：2026 年 8 月 5 日
>
> 重要原则：本文描述的是已经落地的真实实现契约，不是重新设计方案。Coze 版本应在现有项目中合并这些能力，不得另建第二套编辑器、第二套报告数据或平行页面。

> 2026-08-05 最新补充：实验成绩已经调整为“教师人工审核唯一计分”，报告提交不再调用 AI 评分，也不再计算或展示实验误差分。学生端唯一展示的分数是学生自己的预习题答对数和逐题解析，不展示教师成绩、审核决定、通过状态或奖状；教师成绩只保留在教师审核页和教师 Excel。预习题按母题方向与不同考法组织，重做排除全部历史原题并重新排列题目和选项。学生 AI 只解释原理或提醒已发布步骤，在模型调用前拒绝成绩、他人成绩、答案、题库、具体实验数据、标准值和拟合参数探取，并采用 `text/event-stream` 逐段流式显示。Coze 迁移时以上述最新口径覆盖本文较早的评分兼容表述。

## 1. 一句话结论

昨日已将原来的五个普通文本框报告填写方式，升级为覆盖五个正式实验的“结构化类 Word 编辑器”：学生可以在平台内编辑中英文题名与摘要、十个论文式章节、富文本、公式、表格、图片和图注；草稿自动保存到服务器并处理多页面冲突；正式提交后冻结，通过复制历史修订生成 R2、R3；网页、DOCX、PDF 使用同一份结构化报告数据；学生报告严格隔离教师评分和内部评语。该功能已通过自动化测试、真实浏览器流程、PDF/DOCX 渲染检查并部署到生产环境。

## 2. Coze 补充工作的最终目标

Coze 版本完成迁移后，必须同时满足以下结果：

1. 离子阱、量子密钥分发、量子纠缠、金刚石量子计算机、单像素光子成像五个正式实验都使用同一套结构化报告编辑器。
2. 学生在平台内即可完成报告编写，不需要先下载 Word 再上传。
3. 编辑器的体验接近 Word，但只开放受控格式，不允许破坏学校统一版式。
4. 草稿保存在服务器，刷新、重新登录和换设备后仍可继续编辑。
5. 两个标签页同时编辑时不得静默覆盖，必须返回版本冲突并保留本地内容。
6. 正式提交产生不可变的 `SubmissionRevision`；提交后当前草稿冻结。
7. 修改已提交报告时，从 R1 复制形成新草稿，再提交 R2；R1、原评分、原审核和原附件完整保留。
8. 网页预览、DOCX 和 PDF 必须读取同一份结构化报告模型，不得各自拼接不同数据。
9. 学生页面、学生 API、学生 DOCX、学生 PDF 均不得出现教师人工评分、内部评语或审核信息。
10. Coze 版本完成后应通过本文第 16 节的全部验收，而不是只确认页面能打开。

## 3. 昨日完成内容总览

### 3.1 提交范围

昨日核心提交共修改 15 个文件，新增 1145 行、删除 672 行：

| 文件 | 作用 | Coze 迁移要求 |
|---|---|---|
| `ARCHITECTURE.md` | 补充结构化草稿、不可变修订和统一报告模型 | 合并架构约束，不复制主项目专属部署说明 |
| `DEPLOYMENT.md` | 补充报告编辑器部署注意事项 | 按 Coze 运行环境改写路径和启动命令 |
| `README.md` | 补充功能和使用入口 | 合并到 Coze README 的功能说明 |
| `ai_service.py` | 新增中英文题名、摘要、关键词翻译 | 保持 AI 客户端懒加载，不引入平台专有 SDK |
| `platform_app/models.py` | 新增 `ReportDraft` 数据模型 | 必须迁移数据库表和唯一约束 |
| `platform_app/services/report_drafts.py` | 结构化文档定义、清洗、校验、草稿与修订转换 | 必须完整迁移，是后端核心契约 |
| `platform_app/services/reporting.py` | 网页、DOCX、PDF 统一结构化渲染 | 用主版本覆盖旧报告拼装逻辑，但保留 Coze 必需的环境适配 |
| `platform_app/blueprints/student.py` | 草稿、图片、翻译、精确预览、提交、复制修订接口 | 合并路由、身份校验、CSRF 和错误码 |
| `platform_app/blueprints/teacher.py` | 教师报告使用统一正文并追加内部审核信息 | 保持师生导出隔离 |
| `templates/experiment.html` | 新编辑器、章节导航、工具栏、A4 预览 | 替换旧五文本框入口，不保留并行旧 UI |
| `templates/report.html` | 正式报告网页展示 | 统一读取结构化文档 |
| `templates/base.html` | 本地 KaTeX 等资源接入 | 资源必须本地提供，禁止 CDN |
| `static/platform.js` | 自动保存、冲突恢复、内容块序列化、公式/表格/图片交互 | 必须整体核对并合并，不得只复制页面外观 |
| `static/quantum-platform.css` | 编辑器、A4 页面、响应式布局和论文版式 | 与唯一 UI 合并，清理旧报告 CSS |
| `tests/test_learning_flow.py` | 草稿安全、冲突、冻结、复制、导出和隐私测试 | 将测试同步到 Coze 版本并执行 |

### 3.2 实际技术实现说明

最终实现没有引入 Quill、Tiptap、TinyMCE 等第三方富文本框架。当前已验证方案为：

- 浏览器原生 `contenteditable` 负责段落内编辑。
- JavaScript 将编辑区 DOM 序列化为白名单结构化 JSON。
- 服务端再次清洗和验证 JSON，绝不保存任意 HTML。
- KaTeX `0.16.11` 本地静态资源负责公式即时渲染。
- DOCX 使用 `python-docx` 生成。
- PDF 使用 ReportLab 生成。
- 中文 PDF 字体使用项目内的 `static/fonts/simhei.ttf`。
- 所有 UI、脚本、字体和公式资源由 Flask 静态目录直接提供，不依赖外部 CDN。

Coze 实施时不得因为原始方案里提到“开源富文本编辑器”而额外安装一套编辑器。应迁移当前已验证的原生结构化实现。

## 4. 学生端编辑器的完整功能

### 4.1 页面布局

桌面端是三栏工作台：

1. 左侧为报告目录，包含中英文前置信息和十个正式章节。
2. 中间为编辑工作区，显示当前章节和工具栏。
3. 右侧为 A4 论文页面近似预览。

响应式规则：

- 宽屏使用三栏布局。
- 小于约 1280 像素时压缩目录和预览宽度。
- 小于约 980 像素时使用“编辑/预览”切换，避免手机或窄窗口三栏拥挤。
- 小于约 680 像素时章节目录变为横向可滚动导航。

右侧预览是即时近似预览，用于快速检查内容和版面；“最终预览”按钮调用服务端正式 PDF 渲染器，正式 PDF 才是最终分页依据。

### 4.2 可编辑的论文信息

学生可以编辑：

- 中文题名；
- 英文题名；
- 作者信息；
- 中文单位信息；
- 英文单位信息；
- 中文摘要；
- 中文关键词；
- 英文摘要；
- 英文关键词；
- 关键实验结果；
- 是否完成全部必做步骤。

默认单位为：

- 中文：`武汉大学物理科学与技术学院，武汉 430072`
- 英文：`School of Physical Science and Technology, Wuhan University, Wuhan 430072, China`

五个实验的默认英文题名：

| 实验代码 | 英文题名 |
|---|---|
| `ION` | `Ion Trap Experiment` |
| `QKD` | `Quantum Key Distribution Experiment` |
| `ENT` | `Quantum Entanglement Experiment` |
| `NV` | `Diamond Quantum Computer Experiment` |
| `SPI` | `Single-Pixel Photon Imaging Experiment` |

### 4.3 固定的十个章节

报告正文采用固定章节标识和顺序：

| 顺序 | 章节 ID | 中文标题 |
|---:|---|---|
| 1 | `purpose` | 实验目的 |
| 2 | `apparatus` | 仪器设备 |
| 3 | `principle` | 实验原理 |
| 4 | `steps` | 实验步骤 |
| 5 | `raw_data` | 原始数据 |
| 6 | `fit` | 拟合与参数 |
| 7 | `error` | 误差分析 |
| 8 | `discussion` | 讨论 |
| 9 | `conclusion` | 结论 |
| 10 | `references` | 参考资料 |

服务端不接受任意新增章节 ID、重复章节 ID 或篡改章节标题。这样可以确保五个实验、网页、Word 和 PDF 的章节契约一致。

### 4.4 工具栏能力

编辑器工具栏已提供：

- 撤销；
- 重做；
- 加粗；
- 斜体；
- 下划线；
- 上标；
- 下标；
- 有序列表；
- 无序列表；
- 左对齐；
- 居中；
- 右对齐；
- 两端对齐；
- 插入公式；
- 插入表格；
- 插入图片。

不开放任意字体、字号、颜色、绝对定位和浮动排版。学校要求的字体、标题等级、页边距、行距和图表样式由系统控制。

### 4.5 公式

- 学生输入 LaTeX 源码。
- 浏览器通过本地 KaTeX 即时渲染。
- 双击公式块可继续修改源码。
- 公式块可带图注式说明。
- 公式 LaTeX 最长 1000 字符，说明最长 500 字符。
- 浏览器包含常见 Python 表达式兼容处理，例如 `**`、`omega`、`*` 转为适合展示的公式表达。
- PDF 侧处理 `\Omega`、`\omega`、`\pm`、`\times`、`\cdot`、`\le`、`\ge` 和上下标等常见符号，避免中文字体缺字产生方框。

### 4.6 表格

- 可插入和编辑表格。
- 双击表格可调整行列数量。
- 服务端要求每行列数一致。
- 最大 60 行、12 列。
- 单元格最大 1000 字符。
- 表格可填写题注。
- A4 预览、PDF 和 DOCX 使用三线表风格。

### 4.7 图片与图注

- 支持 JPEG、PNG、WebP。
- 图片通过现有安全上传链路保存，返回 SHA-256 资源标识。
- 报告 JSON 只保存 `asset_hash`，不保存任意本地路径或外部 URL。
- 插入图片时，服务端检查哈希必须属于当前草稿。
- 单份草稿最多 30 个图片资源。
- 图片宽度限制在页面宽度的 20% 至 100%。
- 支持左对齐、居中和右对齐。
- 支持图注。
- 双击图片可以修改宽度、对齐方式和图注。

Coze 如果使用对象存储替代本地持久卷，也必须保留 `FileAsset`/SHA-256 注册、归属校验和鉴权媒体读取，不能直接信任前端提交的对象 URL。

### 4.8 实验数据联动

编辑器会把实验版本中已有的信息预填到报告草稿：

- 实验简介预填到“实验目的”；
- 仪器信息预填到“仪器设备”；
- 实验原理预填到“实验原理”；
- 实验步骤预填为有序列表；
- 拟合公式预填到“拟合与参数”；
- 误差分析预填提示语；
- 参考资料预填实验教学材料和本次原始数据/拟合结果说明。

浏览器实验过程中产生的原始数据、拟合参数和拟合图可以进入报告内容；学生可继续补充分析文字。学生代码仍然只能在浏览器 Pyodide 中执行，服务端不得执行学生提交的代码。

## 5. 结构化报告数据契约

### 5.1 顶层 JSON

当前数据版本为 `schema_version = 1`。顶层结构如下：

```json
{
  "schema_version": 1,
  "title_zh": "离子阱实验",
  "title_en": "Ion Trap Experiment",
  "author_name": "学生姓名",
  "student_id": "学生内部标识",
  "affiliation_zh": "武汉大学物理科学与技术学院，武汉 430072",
  "affiliation_en": "School of Physical Science and Technology, Wuhan University, Wuhan 430072, China",
  "abstract_zh": "中文摘要",
  "keywords_zh": "离子阱；拟合",
  "abstract_en": "English abstract",
  "keywords_en": "ion trap; fitting",
  "sections": [],
  "fit_result": {},
  "result_value": "1.04",
  "steps_complete": true
}
```

### 5.2 内容块类型

允许的内容块只有以下几种：

#### 段落、标题、引用

```json
{
  "type": "paragraph",
  "text": "正文",
  "runs": [
    {"text": "加粗内容", "marks": ["bold"]},
    {"text": "普通内容", "marks": []}
  ],
  "alignment": "justify"
}
```

允许的 `type`：`paragraph`、`heading`、`quote`。

允许的文字标记：`bold`、`italic`、`underline`、`superscript`、`subscript`。

允许的对齐方式：`left`、`center`、`right`、`justify`。

#### 公式

```json
{
  "type": "formula",
  "latex": "\\omega = qB/m",
  "caption": "离子回旋频率关系"
}
```

#### 列表

```json
{
  "type": "list",
  "ordered": true,
  "items": ["步骤一", "步骤二"]
}
```

#### 表格

```json
{
  "type": "table",
  "rows": [
    ["序号", "频率", "幅值"],
    ["1", "1.00", "0.82"]
  ],
  "caption": "实验测量数据"
}
```

#### 图片

```json
{
  "type": "image",
  "asset_hash": "64位SHA-256哈希",
  "caption": "拟合曲线",
  "width": 80,
  "alignment": "center"
}
```

### 5.3 安全和容量限制

| 项目 | 限制 |
|---|---:|
| 单个普通正文最大长度 | 20,000 字符 |
| 全文内容块总数 | 240 |
| 单段格式片段数 | 500 |
| 单个格式片段文字 | 5,000 字符 |
| 列表项目数 | 100 |
| 单个列表项目 | 2,000 字符 |
| 表格行数 | 60 |
| 表格列数 | 12 |
| 单元格文字 | 1,000 字符 |
| 图片资源数 | 30 |
| 中文题名 | 200 字符 |
| 英文题名 | 300 字符 |
| 中文摘要 | 5,000 字符 |
| 英文摘要 | 8,000 字符 |
| 中文关键词 | 500 字符 |
| 英文关键词 | 800 字符 |
| 公式源码 | 1,000 字符 |
| 题注 | 500 字符 |

服务端只接受上述白名单节点和属性。以下内容必须拒绝：

- `html`、`script`、`iframe` 等任意 HTML 内容块；
- `onclick`、`style` 等任意 DOM 属性；
- 不属于当前草稿的图片哈希；
- 不规则表格；
- 超出限制的内容；
- 重复、未知或伪造章节；
- 不受支持的格式标记。

## 6. 草稿数据模型

新增数据库表 `report_drafts`，模型为 `ReportDraft`。

| 字段 | 类型/约束 | 说明 |
|---|---|---|
| `id` | `String(64)`，主键 | 草稿 ID |
| `student_id` | 外键 `users.id`，索引 | 草稿所有者 |
| `course_id` | 外键 `courses.id`，索引 | 所属课程 |
| `experiment_version_id` | 外键 `experiment_versions.id`，索引 | 所属不可变实验版本 |
| `source_revision_id` | 可空外键 `submission_revisions.id`，索引 | 从哪个历史修订复制而来 |
| `status` | `String(20)`，索引 | `active` 或 `locked` |
| `content` | JSON | 结构化报告文档 |
| `asset_hashes` | JSON 数组 | 当前草稿允许引用的图片哈希 |
| `lock_version` | Integer | 乐观锁版本，默认 1 |
| `created_at` | 带时区时间 | 创建时间 |
| `updated_at` | 带时区时间 | 更新时间 |

唯一约束：

```text
(course_id, student_id, experiment_version_id)
```

含义是每名学生、每门课程、每个实验版本只能有一个活动记录。正式提交后记录冻结；复制历史修订时，这一记录被明确重新初始化为新的活动草稿，并记录来源修订。

Coze 版本必须使用 PostgreSQL 正式存储，不得改为 `data.json`、SQLite、浏览器 localStorage 或 Supabase 主存储。localStorage 只允许作为保存失败时的临时恢复副本，不能作为权威数据源。

## 7. 草稿生命周期与修订规则

### 7.1 首次打开

1. 根据学生、课程、实验版本查询唯一草稿。
2. 不存在时用不可变 `ExperimentVersion.definition` 生成默认结构化文档。
3. 写入 `report_drafts`。
4. 写审计事件 `report_draft.create`。

### 7.2 自动保存

1. 前端读取当前 `lock_version`。
2. 用户修改后等待 1200 毫秒防抖。
3. 发送完整白名单 JSON 和当前 `lock_version`。
4. 服务端校验草稿仍为 `active`。
5. 服务端比较请求版本和数据库版本。
6. 校验结构化文档和图片归属。
7. 保存成功后 `lock_version + 1`，更新时间，并写审计事件 `report_draft.save`。

### 7.3 多页面冲突

如果请求中的版本已经落后，服务端返回 HTTP 409：

服务层使用内部冲突标识 `report_draft_conflict`，路由层将其稳定转换为以下对前端公开的响应：

```json
{
  "error": "其他页面已修改这份草稿",
  "code": "conflict",
  "draft": {"...": "服务器最新草稿"}
}
```

前端必须：

- 不覆盖服务器内容；
- 将当前本地编辑内容写入 `localStorage` 恢复副本；
- 明确显示冲突状态；
- 允许学生重新载入服务器版本；
- 保留本地内容，供另存或人工合并；
- 不得悄悄选择任意一方覆盖另一方。

恢复键格式：

```text
report-draft-recovery:<实验代码>
```

### 7.4 正式提交

1. 学生必须先完成十道预习题。
2. 页面提交 `draft_id` 和幂等 `request_id`。
3. 服务端重新从数据库读取草稿，不信任前端临时快照。
4. 服务端执行正式提交完整性校验。
5. 将结构化文档转换成历史报告字段兼容结构，同时把完整文档放入 `payload.report_document`；不得再触发 AI 或误差分评分。
6. 创建不可变 `SubmissionRevision`。
7. `request_id` 保证重复请求不会创建重复修订。
8. 提交成功后将草稿状态改为 `locked`。
9. 后续不得原地修改该修订或冻结草稿。

正式提交必须具备：

- 中英文题名；
- 中英文摘要；
- 中英文关键词；
- 关键实验结果；
- 已确认完成全部步骤；
- 非空的实验原理；
- 非空的原始数据；
- 非空的讨论；
- 非空的结论。

### 7.5 复制历史报告形成下一修订

1. 学生在自己的历史报告上点击“复制为草稿”。
2. 服务端校验修订属于当前学生。
3. 复制结构化正文和附件哈希。
4. 设置 `source_revision_id`。
5. 草稿状态改为 `active`。
6. 增加 `lock_version`。
7. 写审计事件 `report_draft.copy_revision`。
8. 学生编辑后再次提交，形成 R2、R3 等新修订。

任何时候都不得覆盖 R1 的正文、评分、教师审核、提交时间和附件引用。

### 7.6 旧报告兼容

历史提交若没有 `payload.report_document`：

- 首次用于显示或复制时，将旧字段 `abstract`、`principle`、`raw_data`、`discussion`、`conclusion`、`result_value`、`fit_result` 转为结构化文档；
- 只在内存中转换或创建新草稿；
- 不覆盖旧提交原始 payload；
- 不迁移、篡改或重新评分旧修订。

## 8. 学生接口契约

所有接口都要求学生身份、当前课程归属和相应实验版本权限；修改类请求继续受现有 CSRF 保护。

### 8.1 获取草稿

```http
GET /student/experiment/<code>/report-draft
```

响应：

```json
{
  "id": "草稿ID",
  "status": "active",
  "lock_version": 2,
  "source_revision_id": null,
  "updated_at": "ISO-8601时间",
  "content": {},
  "asset_hashes": []
}
```

### 8.2 保存草稿

```http
PUT /student/experiment/<code>/report-draft
Content-Type: application/json
```

请求：

```json
{
  "lock_version": 2,
  "content": {"schema_version": 1}
}
```

状态码：

- `200`：保存成功，返回新版本草稿；
- `400`：文档结构、长度、资源引用等校验失败；
- `404`：草稿或实验不存在；
- `409`：其他页面已经修改。

### 8.3 上传报告图片

```http
POST /student/experiment/<code>/report-draft/image
Content-Type: multipart/form-data
```

表单字段：`image`。

成功响应包含：

```json
{
  "asset_hash": "SHA-256",
  "url": "受控媒体地址",
  "lock_version": 3
}
```

草稿冻结、图片格式错误、数量超限或文件校验失败时必须拒绝。

### 8.4 生成英文草稿

```http
POST /student/experiment/<code>/report-draft/translate
Content-Type: application/json
```

请求包含当前 `lock_version` 和完整 `content`。服务端：

1. 校验版本和草稿状态；
2. 校验结构化文档；
3. 懒加载 AI 客户端；
4. 仅翻译中文题名、摘要和关键词；
5. 要求保持事实、数字和结论，不增加不存在的信息；
6. 返回并保存可编辑的英文草稿。

AI 失败返回 503，并且不得破坏已有草稿。英文内容是学生可编辑草稿，正式导出时不再临时调用 AI 改写。

### 8.5 精确 PDF 预览

```http
POST /student/experiment/<code>/report-draft/preview.pdf
Content-Type: application/json
```

服务端校验当前内容后，用与正式 PDF 相同的渲染器返回内联 PDF。预览不会创建正式修订，也不会触发评分。

### 8.6 正式提交

```http
POST /student/experiment/<code>/submit
```

关键表单字段：

- `csrf_token`；
- `request_id`；
- `draft_id`。

服务端从数据库草稿快照创建修订。旧表单字段路径只为历史兼容保留，不应继续作为 Coze 新页面的主提交方式。

### 8.7 复制修订

```http
POST /student/submission/<submission_id>/copy-to-draft
```

JSON 客户端返回新草稿；普通浏览器表单重定向到实验页 `#report`。

### 8.8 报告查看与下载

```http
GET /student/report/<submission_id>
GET /student/report/<submission_id>.docx
GET /student/report/<submission_id>.pdf
```

现有下载 URL 保持不变。所有学生响应必须执行所有权校验，并排除教师内部字段。

## 9. AI 翻译功能

新增 `translate_report_frontmatter(title_zh, abstract_zh, keywords_zh)`：

- AI 客户端必须懒加载；
- 只有学生主动点击时才调用；
- 中文摘要为空时直接提示学生填写，不发送无意义请求；
- 输出要求为严格 JSON；
- 仅生成 `title_en`、`abstract_en`、`keywords_en`；
- 保留原文数字、实验事实和结论；
- 不增加未被中文内容支持的结论；
- AI 输出再次进入结构化文档校验和草稿保存；
- AI 失败不生成虚假回退翻译，不清空学生已有英文内容；
- PDF/DOCX 导出时绝不再次调用 AI。

如果 Coze 版本有自己的模型调用配置，只可适配底层客户端初始化，不得改变上述业务语义，也不得恢复 Coze 专有 SDK 作为领域层依赖。

## 10. 报告生成和版式

### 10.1 单一数据源

以下三个输出必须调用同一份 `report_document`：

- 平台网页正式报告；
- DOCX；
- PDF。

不得在模板、Word 生成器、PDF 生成器中分别从旧字段拼出三份正文。旧字段只用于历史报告转换，不参与新成绩计算。

### 10.2 论文式连续版面

新版取消旧“封面 + 九页卡片”结构，改为连续 A4 论文格式：

1. 页眉和页码；
2. 中英文题名；
3. 作者和中英文单位；
4. 中文摘要和关键词；
5. 英文摘要和关键词；
6. 编号正文；
7. 公式；
8. 三线表和表题；
9. 图片和图注；
10. 讨论、结论和参考资料；
11. 按内容自然分页。

页数不固定，短报告和长报告都应自然分页，不允许硬编码九页或每章节一页。

### 10.3 PDF

- 使用 ReportLab。
- A4 纸张。
- 使用项目本地 `static/fonts/simhei.ttf` 注册中文字体。
- 支持标题、段落、公式、列表、表格、图片、图注和自然分页。
- 页眉包含报告题名，页脚/页眉区域显示页码。
- 处理常用 LaTeX 符号，避免缺字方框。
- 预览 PDF 和正式 PDF 使用同一渲染逻辑。

### 10.4 DOCX

- 使用 `python-docx`。
- A4 页面。
- 上下页边距约 20 mm，左右页边距约 23 mm。
- 固定中英文字体映射。
- 页眉表格和 `PAGE` 域生成页码。
- 标题、段落、表格、图注、公式文本和图片顺序与 PDF 一致。
- 有序列表使用显式编号，保证每个章节从 1 开始，避免 Word 跨章节连续编号。

### 10.5 教师内部报告

教师下载和查看继续使用同一份学生正文，但 `teacher_view=True` 时可以追加独立的内部审核附页或内部审核区域。

隔离要求：

- 学生网页不显示教师人工评分和内部评语；
- 学生 API 不返回相关字段；
- 学生 DOCX/PDF 不包含相关字段；
- 教师内部页面和教师内部导出可以显示；
- 不允许先把敏感字段发送到学生浏览器再用 CSS 隐藏。

## 11. 前端自动保存和交互状态

编辑器根节点使用 `data-report-editor` 标识。前端应完整保留以下状态：

- 正在编辑；
- 保存中；
- 已保存，并显示最近保存时间；
- 保存失败；
- 其他页面已修改；
- 已冻结。

关键行为：

1. 用户输入后标记 dirty。
2. 1200 毫秒防抖后自动保存。
3. 保存期间串行处理，不让旧请求覆盖新请求。
4. 保存失败时保留 dirty 状态和本地恢复副本。
5. 页面关闭前若仍有未保存内容，显示离开提示。
6. 刷新后从服务器恢复；若存在失败恢复副本，应明确提示处理。
7. 正式提交前等待当前保存完成。
8. 如果关键结果、步骤确认或必填章节不完整，不允许正式提交。
9. 409 冲突时不静默覆盖。
10. 精确预览在异步等待前保存按钮引用，避免 `event.currentTarget` 在 `await` 后变为 `null`。

前端只把 DOM 转成结构化 JSON；不得把 `innerHTML` 作为报告正文直接传给数据库。

## 12. 权限、安全和不可变性

Coze 迁移时必须逐项保持：

1. 所有草稿接口仅限学生角色。
2. 学生只能访问自己所在课程和自己的草稿。
3. 学生只能复制自己的历史修订。
4. 图片必须属于当前草稿允许的哈希集合。
5. 修改类请求必须通过 CSRF。
6. 使用 `request_id` 保证正式提交幂等。
7. `SubmissionRevision` 不可原地覆盖。
8. 教师审核不覆盖旧审核，仍使用现有不可变/保留历史规则。
9. 服务端不保存任意 HTML。
10. 服务端不执行学生代码。
11. AI 客户端懒加载。
12. 文件按 SHA-256 内容寻址并通过授权接口读取。
13. 生产数据只进入 PostgreSQL。
14. 任何真实报告、上传文件、密码、Token、Key CSV、`.env.local` 都不得进入代码仓库或交付包。

## 13. Coze 版本迁移实施顺序

### 第一步：备份和差异盘点

- 备份 Coze 当前可运行版本、数据库结构和部署配置。
- 记录 Coze 当前入口、路径前缀、认证方式、静态资源方式和持久存储方式。
- 对照基准提交逐文件比较。
- 保留 Coze 的环境变量和部署外壳，不复制主版本的真实凭据。
- 找出并记录旧报告入口、旧五文本框、旧 CSS 和旧报告生成器。

### 第二步：先迁移领域模型

- 增加 `ReportDraft` 模型和 PostgreSQL 表。
- 创建唯一约束和索引。
- 保持 `SubmissionRevision` 的原有不可变与幂等约束。
- 如果 Coze 使用迁移工具，生成正式迁移；如果项目启动时统一建表，也要验证旧库升级不会丢数据。
- 先运行模型级测试，再继续 UI。

### 第三步：迁移结构化报告服务

- 完整迁移 `platform_app/services/report_drafts.py`。
- 不削弱白名单和容量限制。
- 保留旧 payload 转换函数。
- 保留 `validate_fit_result` 校验。
- 保留审计事件。

### 第四步：迁移接口和权限

- 合并学生草稿、图片、翻译、预览、提交和复制修订接口。
- 复用 Coze 版本已有登录、角色、课程归属和 CSRF 机制。
- 不得只依靠前端隐藏按钮实现权限控制。
- 保持错误码和冲突响应，便于直接复用前端逻辑。

### 第五步：迁移统一报告生成器

- 迁移 `report_document`、`build_docx`、`build_pdf`。
- 保留现有下载 URL。
- 保留教师/学生输出隔离。
- 确认 Coze 运行镜像包含 ReportLab、python-docx、Pillow 等现有依赖。
- 确认 `static/fonts/simhei.ttf` 实际打包且运行时可读。

### 第六步：迁移唯一编辑器 UI

- 将 `templates/experiment.html` 中旧报告区替换为新编辑器。
- 合并 `static/platform.js` 中报告编辑器逻辑。
- 合并 `static/quantum-platform.css` 中编辑器和论文样式。
- 合并 `templates/base.html` 本地 KaTeX 资源加载。
- 同步 `templates/report.html`。
- 删除失效的旧报告输入框、旧事件监听、旧 CSS 和旧模板分支。
- 不保留“新版/旧版切换”或第二个报告入口。

### 第七步：处理 Coze 环境差异

- 所有 URL 使用 Flask `url_for` 或当前路径前缀工具，不硬编码 `/` 根路径。
- 反向代理必须正确传递协议和主机信息。
- 如果 Coze 部署在子路径，静态资源、表单、API、图片和 PDF URL 都必须带正确前缀。
- Cookie、会话和 CSRF 必须在实际 iframe/反向代理场景测试。
- 图片上传必须使用持久存储。临时容器磁盘会在重启或重新部署后丢失，不能作为正式方案。
- 如果没有持久挂载卷，使用对象存储，并维持 SHA-256、数据库注册、所有权校验和授权读取。

### 第八步：迁移测试并逐层验证

- 先执行服务测试。
- 再执行完整 pytest。
- 再运行项目验证脚本。
- 再在 Coze 预览环境执行真实浏览器流程。
- 最后才发布正式版本。

## 14. Coze 环境的条件适配说明

当前主版本已经移除 Coze 专有 SDK，使用 Flask、Jinja、静态文件和 PostgreSQL。迁移到 Coze 托管环境时，应适配部署外壳，不应让业务层重新依赖 `coze-coding-dev-sdk`。

如果 Coze 外层是 iframe，并且认证确实需要外层 Token 传播，可在会话入口做最小适配，但必须满足：

- 只有存在合法 Token 时才追加或传播；
- 不把 Token 写进日志、HTML、localStorage 或报告；
- HTTPS iframe Cookie 根据实际浏览器要求设置 `SameSite=None; Secure`；
- 正常直连登录路径仍可用；
- 不把 iframe Token 逻辑散布到草稿、图片、预览、下载等每个业务接口；
- 先通过真实 Coze 预览页验证，再决定是否需要这层适配。

这只是 Coze 外壳的条件性兼容，不是主报告功能的一部分。若 Coze 版本当前登录已经稳定，不要主动引入新的 Token 分支。

## 15. 已完成的验证证据

昨日主版本已经完成：

- `python scripts/verify_project.py`：通过；
- `pytest -q`：`56 passed in 195.01s`；
- 五个实验 `ION`、`QKD`、`ENT`、`NV`、`SPI` 页面均返回 200 并包含新编辑器；
- 自动保存后刷新恢复：通过；
- 过期 `lock_version` 返回 409：通过；
- 恶意 `html` 内容块被 400 拒绝：通过；
- 正式提交后草稿冻结：通过；
- 从 R1 复制为活动草稿且 R1 不变：通过；
- 精确 PDF 预览返回有效 `%PDF`：通过；
- 学生 DOCX 返回有效 ZIP/DOCX：通过；
- 学生报告不泄露教师内部评语：通过；
- 真实浏览器检查编辑器、A4 预览、KaTeX 公式和精确 PDF 新窗口：通过；
- 浏览器控制台错误：0；
- 代表性 PDF/DOCX 包含中英文摘要、公式、表格、图片、图注、页眉页码和自然分页：通过；
- PDF 逐页渲染检查：通过；
- 发现并修复公式缺字方框；
- 发现并修复 DOCX 列表跨章节连续编号；
- 发现并修复异步 PDF 预览按钮引用失效；
- 发现并修复浏览器公式表达式规范化问题。

昨日代码已推送至 GitHub `origin/main`。生产部署版本标识为 `report-editor-20260804-162723`，部署后容器内健康与就绪检查均为 200，数据库、文件和 AI 检查为正常。正确公网入口使用 Nginx 路径前缀；直接访问公网 IP 根路径的 `/health` 返回外层 404 属于路径隔离，不代表应用失败。

以上是主版本证据。Coze 版本迁移后必须重新执行自己的测试，不能把主版本通过当成 Coze 环境已经通过。

## 16. Coze 版本验收清单

### 16.1 基础页面

- [ ] 五个正式实验都只有一个报告编辑入口。
- [ ] 旧五文本框和旧报告 UI 已删除。
- [ ] 桌面端目录、编辑区、A4 预览布局正常。
- [ ] 窄屏可以在编辑和预览之间切换。
- [ ] 所有 KaTeX、字体、图标、JS、CSS 均从本地加载，无 CDN 失败。

### 16.2 编辑能力

- [ ] 中英文题名、摘要和关键词可编辑。
- [ ] 十个章节顺序正确。
- [ ] 加粗、斜体、下划线、上下标正常。
- [ ] 有序/无序列表正常。
- [ ] 四种对齐正常。
- [ ] 公式插入、渲染和再次编辑正常。
- [ ] 表格插入、编辑、调整行列和三线表预览正常。
- [ ] 图片上传、显示、宽度、对齐和图注正常。
- [ ] 撤销和重做正常。
- [ ] 复制粘贴不会存入任意 HTML 或脚本。

### 16.3 保存与恢复

- [ ] 输入后约 1.2 秒自动保存。
- [ ] 页面显示保存中、已保存、失败、冲突和冻结状态。
- [ ] 刷新后内容恢复。
- [ ] 退出登录再登录后内容恢复。
- [ ] 另一设备登录后内容恢复。
- [ ] 断网保存失败时本地内容不丢失。
- [ ] 两个标签页编辑时旧版本收到 409，不静默覆盖。
- [ ] 页面存在未保存内容时关闭有提示。

### 16.4 修订流程

- [ ] 不完整报告不能正式提交。
- [ ] 重复点击提交只产生一个修订。
- [ ] 提交成功后草稿冻结。
- [ ] R1 内容、附件、评分和审核不可原地修改。
- [ ] R1 可复制为新活动草稿。
- [ ] 修改后提交形成 R2。
- [ ] R1 和 R2 均可独立查看和下载。
- [ ] R2 不覆盖 R1 的任何历史信息。

### 16.5 报告一致性

- [ ] 网页、DOCX、PDF 题名一致。
- [ ] 中英文摘要和关键词一致。
- [ ] 章节顺序一致。
- [ ] 数值、拟合参数和公式一致。
- [ ] 表格内容和题注一致。
- [ ] 图片和图注一致。
- [ ] 短报告自然分页正常。
- [ ] 长报告多页分页正常。
- [ ] PDF 为 A4 并有正确页眉页码。
- [ ] DOCX 打开无修复提示。
- [ ] 中文无乱码、公式无方框。

### 16.6 权限和恶意输入

- [ ] 学生 A 不能读取或修改学生 B 草稿。
- [ ] 学生不能复制他人修订。
- [ ] 教师接口和学生接口权限不混用。
- [ ] 学生网页/API/DOCX/PDF 均无教师内部评分和评语。
- [ ] 任意 HTML 内容块被拒绝。
- [ ] `<script>`、事件属性和 iframe 无法保存。
- [ ] 超长公式被拒绝。
- [ ] 超限表格被拒绝。
- [ ] 不规则表格被拒绝。
- [ ] 未授权图片哈希被拒绝。
- [ ] 超过 30 张图片被拒绝。
- [ ] CSRF 缺失的修改请求被拒绝。

### 16.7 部署与持久性

- [ ] PostgreSQL 中存在 `report_drafts` 表和唯一约束。
- [ ] 容器重启后草稿仍在。
- [ ] 重新部署后草稿和图片仍在。
- [ ] 子路径部署下静态资源、API、图片、PDF 地址全部正确。
- [ ] iframe/代理环境登录和 CSRF 正常。
- [ ] 健康检查访问的是实际应用路径，不用外层根路径 404 判断失败。
- [ ] 部署包不含 `.env.local`、真实报告、上传文件、密码、Key 或 Token。

### 16.8 必跑命令

```powershell
python scripts/verify_project.py
pytest -q
```

如果 Coze 的项目根目录、Python 命令或启动方式不同，可以调整命令外壳，但不能省略等价验证。

## 17. 禁止事项与常见误区

1. 禁止新建第二套“Coze 专用编辑器”。
2. 禁止保留旧五文本框作为隐藏兼容入口。
3. 禁止把 HTML 原样存进数据库。
4. 禁止只做页面外观，不迁移草稿服务、乐观锁和修订规则。
5. 禁止用 localStorage 代替服务器草稿。
6. 禁止用临时容器磁盘长期保存报告图片。
7. 禁止在导出时临时调用 AI 翻译，造成预览和正式报告不一致。
8. 禁止让学生响应携带教师内部字段后再前端隐藏。
9. 禁止原地更新已提交修订。
10. 禁止把 PostgreSQL 改回 JSON、SQLite 或 Supabase。
11. 禁止恢复 Coze 专有 SDK 到业务服务层。
12. 禁止使用外部 CDN 加载 KaTeX、字体或 UI 资源。
13. 禁止硬编码公网域名或根路径，导致子路径部署资源 404。
14. 禁止只看 `/health` 一个 URL 就判断部署成功；必须确认请求经过正确路径前缀并进入应用。
15. 禁止在未通过真实浏览器、下载和持久性检查前宣布完成。

## 18. 失败排查顺序

### 页面没有新编辑器

1. 检查 `templates/experiment.html` 是否真正替换旧报告区。
2. 检查浏览器 HTML 是否包含 `data-report-editor`。
3. 检查 `static/platform.js` 是否为新版并成功加载。
4. 检查控制台是否有 JS 语法或资源 404。
5. 检查是否仍渲染了旧模板分支。

### 自动保存失败

1. 看 PUT 请求的实际 URL、状态码和响应 JSON。
2. 检查 CSRF。
3. 检查学生课程和实验版本归属。
4. 检查 `lock_version` 是否落后。
5. 检查 JSON 是否包含不支持的块或未授权图片。
6. 检查 PostgreSQL 表和事务日志。

### 图片刷新后消失

1. 检查文件是否写入持久卷或对象存储。
2. 检查 `FileAsset`/哈希记录是否存在。
3. 检查草稿 `asset_hashes` 是否包含该哈希。
4. 检查媒体 URL 是否包含 Coze 子路径。
5. 检查读取接口是否因 Cookie、权限或代理协议失败。

### PDF 中文乱码或公式方框

1. 检查 `static/fonts/simhei.ttf` 是否被打包。
2. 检查运行用户是否可读该文件。
3. 检查 ReportLab 是否成功注册字体。
4. 检查公式转换是否输出了字体不支持的 Unicode。
5. 将 PDF 渲染成逐页图片检查，不只看生成成功。

### Coze 预览登录后接口 401/403

1. 确认登录 Cookie 是否在 iframe 中被浏览器保存和发送。
2. 确认代理是否传递 HTTPS 协议头。
3. 确认 Cookie 的 `Secure`、`SameSite` 和路径。
4. 确认 CSRF Token 来自同一会话。
5. 只有证明确实需要时才增加外层 Token 适配。

### `/health` 返回 404

1. 确认访问的是应用真实路径前缀下的 `/health`。
2. 检查 404 响应来自 Nginx 外层还是 Flask 应用。
3. 查看代理 location 规则。
4. 用应用内部端口验证健康接口。
5. 外层根路径 404 但正确前缀 200 时，应用本身是正常的。

## 19. 可直接复制给 Coze 的实施提示词

下面这段可以直接交给 Coze。建议同时把本文档和主版本基准提交对应文件提供给它。

```text
你现在要在现有 Coze 版本的武汉大学量子实验教学平台中，合并“论文式实验报告与平台内类 Word 编辑器”功能。这不是重新设计，也不是新建独立 Demo。请先完整检查当前项目结构、数据库模型、路由、模板、静态资源、认证、CSRF、报告导出和部署方式，再以现有主版本提交 aef025ceca28bcfc6083f79d8e89949a754118e6 为实现基准进行合并。

必须遵守：
1. 唯一 UI 仍是 templates/ + static/quantum-platform.css，不允许创建第二套编辑器、第二套页面或新版/旧版切换。
2. 五个正式实验 ION、QKD、ENT、NV、SPI 全部启用同一套编辑器。
3. 实际实现是原生 contenteditable + 白名单结构化 JSON + 本地 KaTeX，不要另装 Quill、Tiptap、TinyMCE。
4. 新增 ReportDraft PostgreSQL 模型：学生/课程/实验版本唯一，包含 source_revision_id、status、content、asset_hashes、lock_version 和时间字段。
5. 完整迁移 platform_app/services/report_drafts.py 的 schema_version=1、固定十章节、内容块白名单、长度限制、图片归属校验、默认文档、旧 payload 转换、保存和复制修订逻辑。
6. 报告只保存结构化 JSON，不保存任意 HTML；拒绝 script、iframe、html 块、异常表格、超长公式和未授权图片哈希。
7. 实现 GET/PUT report-draft、图片上传、AI 翻译、精确 PDF 预览、正式提交、copy-to-draft 接口，并保留学生身份、课程归属、CSRF、乐观锁和正确的 400/404/409/503 响应。
8. 自动保存防抖约 1200ms；显示保存中、已保存、失败、冲突、冻结；409 时不能覆盖，必须保留本地恢复副本。
9. 正式提交必须从服务器草稿快照创建不可变 SubmissionRevision，继续使用 request_id 幂等；提交成功后草稿 locked。
10. 修改历史报告必须从 R1 复制为活动草稿，再提交 R2；不得覆盖 R1、旧评分、旧审核和附件。
11. 网页、DOCX、PDF 统一读取 report_document；使用连续 A4 论文格式、中英文题名摘要、编号正文、公式、三线表、图片图注、页眉页码和自然分页。
12. 保留 static/fonts/simhei.ttf、本地 KaTeX 和报告依赖；禁止外部 CDN。
13. 学生页面、API、DOCX、PDF 不能包含教师评分、内部评语或审核字段。教师视图只能在 teacher_view 模式追加独立内部审核内容。
14. 学生代码只在浏览器 Pyodide 运行；AI 客户端懒加载；不得恢复 coze-coding-dev-sdk 到业务层。
15. 生产业务数据只使用 PostgreSQL，不得改成 data.json、SQLite、localStorage 或 Supabase。图片必须持久化；若没有持久卷则使用对象存储，但保留 SHA-256 注册、归属校验和鉴权读取。
16. 所有 URL 适配 Coze 反向代理和子路径，不硬编码根路径。只有真实 iframe 登录证明确有需要时，才做最小 Cookie/Token 外壳适配，不要污染业务接口。
17. 合并完成后删除旧五文本框、旧报告 JS、旧 CSS、旧模板分支和失效文档，不能留下平行实现。
18. 不得读取、输出、提交或打包 .env.local、密码、Token、Key、真实报告和上传文件。

实施顺序：
A. 备份当前 Coze 版本并给出差异清单。
B. 迁移数据库模型和表。
C. 迁移结构化报告服务和单元测试。
D. 迁移学生/教师路由、权限、CSRF 和幂等逻辑。
E. 迁移统一网页/DOCX/PDF 渲染器。
F. 替换唯一报告 UI，合并 JS/CSS 和本地资源。
G. 清理旧实现。
H. 运行验证和真实浏览器验收。

必须执行并报告：
- python scripts/verify_project.py
- pytest -q
- 五个实验页面均能打开且含 data-report-editor
- 自动保存和刷新恢复
- 双标签页 409 冲突且不丢本地内容
- 断网/保存失败恢复
- 公式、表格、图片、图注、撤销重做
- 精确 PDF 预览
- R1 提交、冻结、复制、修改、提交 R2，确认 R1 不变
- 学生和教师报告隐私隔离
- 网页/DOCX/PDF 内容一致
- PDF 逐页渲染检查 A4、中文、公式、表格、图片、页眉页码、自然分页
- Coze 实际预览 URL 下登录、静态资源、API、图片、PDF、CSRF 和容器重启持久性

输出要求：
1. 先说明当前 Coze 版本与基准实现的真实差异。
2. 列出实际修改的文件和数据库变化。
3. 给出测试命令、通过数量、关键接口状态和浏览器验收证据。
4. 明确列出仍未验证或被阻塞的项目，不得用“应该可以”代替证据。
5. 测试未通过时继续定位根因并修复，不要用兼容分支、临时开关或复制一套新页面绕过。
6. 在全部本地和预览验收完成前，不要宣称部署成功。
```

## 20. 交付完成定义

只有同时满足以下条件，才能认为 Coze 版本补充完成：

1. 功能不是 Demo，而是合并到唯一学生实验流程。
2. 数据已进入 PostgreSQL 并通过重启/重新部署持久性验证。
3. 自动保存、冲突、冻结、复制修订完整闭环。
4. 五个实验全部工作。
5. 网页、DOCX、PDF 一致且版式经过视觉检查。
6. 学生报告无教师内部信息。
7. 旧 UI 和旧逻辑已清理。
8. 自动化测试通过。
9. Coze 真实预览浏览器流程通过。
10. 部署包无密钥、真实数据和临时文件。

满足以上十项后，再进行正式发布，并记录发布版本、时间、回滚点、健康检查实际 URL 和最终验收结果。
